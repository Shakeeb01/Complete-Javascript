// Type Converstion:
// It is the process of changing the datatype of value into another datatype.


// Converting String into number

let age = window.prompt('What is your age?');

age = Number(age); // Converting string into number
age = String(age); // Number nuber into string.
age = Boolean(age); // Converting string into Boolean
age+=1;
console.log(age)
